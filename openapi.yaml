---
openapi: 3.0.3
info:
  title: customers API
  version: 1.0-SNAPSHOT
servers:
- url: http://localhost:8090
  description: Auto generated value
- url: http://0.0.0.0:8090
  description: Auto generated value
paths:
  /customers:
    get:
      tags:
      - Example Resource
      responses:
        "200":
          description: OK
          content:
            text/plain:
              schema:
                type: string
  /customers/{id}:
    get:
      tags:
      - Example Resource
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            text/plain:
              schema:
                type: string
